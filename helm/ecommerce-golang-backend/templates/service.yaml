apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecommerce-golang-backend.fullname" . }}
  labels:
    {{- include "ecommerce-golang-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecommerce-golang-backend.selectorLabels" . | nindent 4 }}
